#!@LINUXDROID_PREFIX@/bin/sh

if [ "${1}" = "remove" ]; then
    if [ -d "@LINUXDROID_PREFIX@/share/applications" ]; then
        rm -f "@LINUXDROID_PREFIX@/share/applications/mimeinfo.cache"
        rmdir --ignore-fail-on-non-empty "@LINUXDROID_PREFIX@/share/applications"
    fi
fi

exit 0
